# U-Tower Parking Bot 옵션
# 값은 켜기/끄기, on/off, true/false, yes/no, 1/0 모두 사용할 수 있습니다.

# 개인 입차/출차임박 알림 중복방지
# 켜기: 같은 차량/같은 입차시각 개인 알림은 한 번만 보냅니다.
# 끄기: 코드가 실행될 때마다 조건에 맞는 개인 알림을 매번 보냅니다.
개인중복방지=켜기

# 마스터 입차 알림 중복방지
# 켜기: 마스터 입차 현황은 차량 목록이 바뀔 때만 보냅니다.
# 끄기: 코드가 실행될 때마다 현재 입차 차량을 매번 보냅니다.
# 최근출차는 이 옵션과 무관하게 최근출차조회분 범위를 매번 다시 조회하고,
# 같은 차량/같은 출차시각은 alert_log 기준으로 한 번만 보냅니다.
마스터중복방지=켜기

# 마스터 TELEGRAM_CHAT_ID로 가는 입차 요약/최근 출차 알림
어드민알림=켜기

# 개인 알림 전체 스위치입니다. 끄면 입차알림/출차임박알림도 모두 꺼집니다.
개인알림=켜기

# vip_list.txt의 각 채팅 ID로 가는 개인 입차 알림
입차알림=켜기

# vip_list.txt의 각 채팅 ID로 가는 개인 출차 임박 알림
# 출차임박/경과 알림은 Cloudflare Worker가 5분마다 KV status를 확인해 보내므로
# 여기서는 끕니다 (켜면 정각 실행 시 중복 알림이 갈 수 있습니다).
출차임박알림=끄기

# 출차시간까지 이 값 이하로 남으면 개인에게 출차 임박 알림을 보냅니다.
# Worker 쪽 기준(30분)은 cloudflare/src/worker.js의 IMMINENT_MINUTES에서 조정합니다.
출차임박알림분=60

# 최근 출차 알림 조회 범위(분). 600분=10시간, 테스트 후 60으로 돌리면 됩니다.
최근출차조회분=600
